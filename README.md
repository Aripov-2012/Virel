# Loopra (Django)

Небольшой блог/соц‑лента на Django.

## Быстрый старт

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

## Примечания
- Медиа-файлы сохраняются в `media/`.
- Шаблоны лежат в `templates/`.
